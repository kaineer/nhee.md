<div class="list">
  {% for link in links %}
  <a href="{{ link.url }}" class="list-item">
    <div class="list-title">{{ link.title }}</div>
    <div class="list-description">{{ link.description }}</div>
  </a>
  {% endfor %}
</div>
