<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ page.title }}</title>
    <link rel="stylesheet" href="{{ root }}/assets/root.css" />
    <link rel="stylesheet" href="{{ root }}/assets/style.css" />
    <link rel="stylesheet" href="{{ root }}/assets/navbar.css" />
    <link rel="stylesheet" href="{{ root }}/assets/kanji.css" />

    <!-- Подключаем красивый японский шрифт -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@400;700&display=swap" rel="stylesheet">
  </head>
  <body>
    {% include "partials/navbar.j2" %}
    <div class="card">
      <h1 class="title">{{ page.title }}</h1>
      <div class="kanji-container">
        {% include "partials/kanji.items.j2" %}
      </div>
      {% if crumb_levels %}
        {% set next_items = crumb_levels[-1] | selectattr("next") | list %}
        {% if next_items %}
          <p class="kanji-next">
            <a href="{{ root }}{{ next_items[0].url }}">{{ next_items[0].slug }} →</a>
          </p>
        {% endif %}
      {% endif %}
    </div>
    <script src="{{ root }}/assets/kanji-tap.js"></script>
  </body>
</html>
