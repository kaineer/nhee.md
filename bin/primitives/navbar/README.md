# navbar item
* on creation gets subdir path. Example: `"foo/bar/"` or `"foo"` or `"."`
* calculates:
  * upper level dir (upper)
  * lower level dir (lower)

# navbar data

* on creation stores root dir
* add_meta adds one item using metafile directory path
