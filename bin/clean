#!/usr/bin/env bash

echo -n " $(ansi white bold)»$(ansi reset) Remove all src/**/*.html files (except templates)"
for f in $(fd -e html --no-ignore . src | grep -v templates); do
  rm $f
done
echo "$(ansi green)done$(ansi reset)"
