#!/usr/bin/env bash
git add .
MESSAGE="Save on $(date +%'Y.%m.%d -- %H:%M:%S')" 
echo -n " $(ansi white bold)»$(ansi reset) Commit and push > $MESSAGE.."
git commit -m "$MESSAGE" &>/dev/null
git push origin master &>/dev/null
echo "$(ansi green)done$(ansi reset)"
